mixin video(video)
        div
            h4 
                a(href=`/videos/${video.id}`)=video.title
            ul 
                li #{video.rating} /5
                li #{video.connebts} comments.
                li Posted #{video.creatdAt}
                li #{video.views} views